services:
  app:
    build: .
    container_name: arcontrole-react

    user: "1000:1000"

    network_mode: host

    volumes:
      - .:/app
      
    environment:
      EXPO_DEVTOOLS_LISTEN_ADDRESS: 0.0.0.0

    command: npm run start

    stdin_open: true
    tty: true
